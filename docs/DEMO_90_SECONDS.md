# ThinkBud 90 秒演示脚本

这是一段成人扮演学习者的交互演示。重点是亲手走过“得到帮助”和“独立完成”的区别。页面响应为预设规则，不调用真实 AI；完整慢速体验约 3 分钟。

## 演示前

直接打开[已发布的产品与流程展示](https://jeffreyliu0131.github.io/thinkbud-ai/)，或进入[数学交互练习](https://jeffreyliu0131.github.io/thinkbud-ai/#/practice)。不需要安装、账号或真实后台。需要从源码本地复现时，按 [README 的静态构建与预览步骤](../README.md#quick-start)操作。

页面默认跟随浏览器语言，也可在页头选择 **中文 / English / 跟随系统**。点击 **开始数学体验 / Try the maths demo**。确认成人体验，保留默认“连续两次困难后，换例子解释”。需要演示刷新恢复时勾选本机保存；重新演示使用页面的“清除本次记录并重新开始”。以下步骤只验证预设的浏览器交互，不是登录、语音或真实 AI 服务的使用教程。


## 扩展展示的浏览顺序 · 2026-09-22

本节对应 2026-09-22 已发布的扩展展示（应用源码 `3c2e919`）；精确版本见 README 发布记录。原来的 90 秒数学实操仍然可用，新首页提供另一条完整的项目浏览路径：

1. **先看三科差异。** 首屏切换数学／语文／英语预览。进入“学科与题型”，选择 1–3 或 4–6 年级、具体题型，查看题目、策略、学生尝试和教练下一步。24 个题型示例来自对源码策略的展示性编写，不是模型输出记录或逐年级课程目录。
2. **对比教学决策。** 在同一题型切换“我卡住了／我尝试了一步／我想结束”。语文作文只给构思框架、不代写句子，也不自动追加另一篇作文；英语可说明词义和语法规则，由学生自己应用。普通对话的变式题按学生主动请求触发。
3. **需要实操时进入数学实验。** 使用下文原有流程，区分引导、独立作答与延迟检查。这是另一个确定性实验路径，不是所有学科的统一验收方式。
4. **查看知识与思考。** 思考链记录一次对话；知识点目录直接来自项目 69 项受控词表。选择概念、增加积极／困难／中性信号，观察项目 BKT 公式的内部估计；切换概念会重置这个示例，不写入账户、本地学习记录或数学练习观察。
5. **追到 AI、RAG 与实现。** 查看上下文和输出检查的分工，再用按钮跳到原有合成检索状态。故障和无结果不展示引用；最后从实现地图进入对应源码，核对页面运行、服务端代码、适配层及待验证事项。

模块导航随阅读位置更新，可直接跳转。页面中英文切换保留当前题型和学习者状态；英语学科原始辅导仍是中文解释，英文 UI 只是展示译文。

## 现场顺序

1. **0–15 秒：说明问题。** “ThinkBud 面向小学语文、数学、英语的思考引导。今天展示四年级数学的一个预设流程：跟着做完，并不等于自己会做。”
2. **15–40 秒：让系统看到困难。** 在 `6 × (10 + 4)` 的第一格连续输入两次 `5`，观察系统换成苹果例子，而不是直接写原题答案。指出右侧“2 次不匹配 · 1 次例子”。接着依次填写 `6`、`60`、`24`、`84`，完成四步引导。
3. **40–65 秒：撤去提示。** 点击“开始独立迁移”，面对新题 `7 × (20 + 3)`，填写展开式 `7, 20, 7, 3`，再填写 `161`。这里不能只提交最终答案跳过展开。“我需要帮助，回去练习”会保留未完成记录，再换一组题。答错后再完成，也会明确记作“重试后完成”。
4. **65–90 秒：展示完成边界。** 正式复测仍要等 24 小时；打开“不计入记录”的预览，填写 `5, 10, 5, 7`，再填写 `85`。预览完成后，右侧仍显示“等待间隔后再检查”。导出 JSON 可核对两段正式观察，里面没有预览结果或原始答案。
5. **追问时再看技术证据。** 回到“项目展示”，展开“查看实现与证据”，切换预设检索故障/无结果，观察引用消失。合成报告验证指定规则，不证明实时模型质量、三科互动覆盖或学习效果。

## 可重复的验收步骤

- **输入与重新尝试：** 空提交不推进；`abc` 显示整数格式错误且不计作答；数值不匹配保留重试；另一种帮助策略在两次错误后展示一步提示。
- **键盘与暂停：** Enter 提交，Tab 顺序经过四个乘数框；暂停禁用提交并保留当前输入。阶段完成后焦点进入下一阶段卡片。
- **本地恢复：** 开启保存后刷新 `/#/practice`，恢复已提交步骤与原有次数；未保存时刷新回到起点；未提交的输入不写入存储。
- **手机：** 在 390px 和 320px 宽度检查首页、四格展开式和提交按钮；输入框有标签，页面可缩放，无页面横向溢出。
- **报告与深链：** `/#/?rag=degraded` 刷新后保持预设故障选择；未知路由回首页。实际报告 URL 必须是对应 base 下的 200 JSON。报告损坏或 503 时，界面提示错误并可重试，数学体验仍可进入。

[当前验收结果与发布版本](../README.md#static-publication-verification--2026-09-21) · [截图](showcase/README.md) · [运行方法](OPERATIONS.md)

## 双语验收补充

首次访问按浏览器语言列表中的首个中文或英文偏好展示；没有支持的语言时显示英文。中文地区变体统一为简体中文。页头的手动选择只保存在本设备，刷新及切换页面后仍保留；选择“跟随系统”可恢复自动识别。

在题目中先填写一个数字但不提交，切换中英文，确认填写内容和观察次数未变。再检查错误提示、换例子说明、独立作答、复测日期及恢复提示都使用当前语言。语言选择不改变导出的观察字段或正式复测门槛，不代表新增了英语学科辅导。
